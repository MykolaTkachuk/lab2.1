#include <stdlib.h>
#include <stdio.h>
#include <math.h>
int main()
{
 double x;
 double y;
      printf("Enter x:");
      scanf("%lf", &x);
         if( -10<=x && x < -2)
            y = -x*x*x+1;
            else if ( x >= 3 && x < 8 )
            y = 1/(2*x);
            else if ( x >= 12 && x < 20 )
            y = (x*x*x)/sqrt(x-1);
            else
            y = 0.0;
 system("cls");
            printf("x = %lf", x);
            printf(" \ny = %lf", y);
 return 0;
}
